# P1115

## 暴力怎么写

前缀和 : O(n^2) 过40pts

## 瓶颈在哪里？

对于 $100\%$ 的数据，保证 $1 \leq n \leq 2 \times 10^5$，$-10^4 \leq a_i \leq 10^4$。

应该最终解 是 O(n)

## 正解消掉了那层复杂度？？

O(n^2) -> O(n)

O(n) -> DP

## 下次看到什么想到此题？

give a seq,求出连续且非空的一段使得这段和最大，输出max {sum}

## DP

$$ f(i) = max(f(i-1) + a_i,a_i) $$
