#include <bits/stdc++.h>
using namespace std;

const int maxn = 1e6 + 5;
int n;
int a[maxn], b[maxn];
int c[maxn];
int debug = 1;
int main(int argc, char const *argv[])
{
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i];
    }
    for (int i = 1; i <= n; i++)
    {
        cin >> b[i];
    }

        return 0;
}
