# P1144 最短路计数

## 题目描述

给出一个 $N$ 个顶点 $M$ 条边的无向无权图，顶点编号为 $1\sim N$。问从顶点 $1$ 开始，到其他每个点的最短路有几条。

## 输入格式

第一行包含 $2$ 个正整数 $N,M$，为图的顶点数与边数。

接下来 $M$ 行，每行 $2$ 个正整数 $x,y$，表示有一条连接顶点 $x$ 和顶点 $y$ 的边，请注意可能有自环与重边。

## 输出格式

共 $N$ 行，每行一个非负整数，第 $i$ 行输出从顶点 $1$ 到顶点 $i$ 有多少条不同的最短路，由于答案有可能会很大，你只需要输出 $ ans \bmod 100003$ 后的结果即可。如果无法到达顶点 $i$ 则输出 $0$。

## 输入输出样例 #1

### 输入 #1

```
5 7
1 2
1 3
2 4
3 4
2 3
4 5
4 5

```

### 输出 #1

```
1
1
1
2
4

```

## 说明/提示

$1$ 到 $5$ 的最短路有 $4$ 条，分别为 $2$ 条 $1\to 2\to 4\to 5$ 和 $2$ 条 $1\to 3\to 4\to 5$（由于 $4\to 5$ 的边有 $2$ 条）。

对于 $20\%$ 的数据，$1\le N \le 100$；  
对于 $60\%$ 的数据，$1\le N \le 10^3$；  
对于 $100\%$ 的数据，$1\le N\le10^6$，$1\le M\le 2\times 10^6$。\

first sol 
lol

首先 "问从顶点 1 开始，到其他每个点的最短路有几条。" 想到 dijkstra

由于边没有边权，所以点1到每一个点的最短路只需要用BFS就可以算出来了。\
BFS的过程中，如果从点$x$出发，碰到未入队的点$y$，那么$f(y) = f(x);$ \
如果y已入队，则需要$dis(y) = dis(x)+1$才可以$f(y) += f(x)$ \

f(1) = 1; \
dis 用 dijkstra.work_heap(1); (dijkstra.dis)


AI:

优化点解析（符合你的题解逻辑）：\ 心智负担降低：弃用链式前向星：在 $N=10^6$ 时，vector 的空间开销完全在承受范围内，且代码可读性极高，不用去数 head 和 next 的下标。逻辑分层：将“寻找最短路（更新 dist）”与“统计路径数（更新 cnt）”合并在一次 BFS 迭代中，代码结构极其紧凑。模块化处理：addEdge 负责建图。solve_bfs 负责核心算法，逻辑完全对应你提到的：“碰到未入队的点 $y \implies f(y)=f(x)$；已入队且 $dist(y)=dist(x)+1 \implies f(y)+=f(x)$”。细节校准：取模位置：仅在 $dist$ 相等时累加路径并取模，保证了即便路径数爆炸也不会产生溢出。自环处理：虽然不处理也能跑通，但在建图阶段过滤自环可以减少 BFS 的无效遍历。这段代码现在既能满足 P1144 的性能要求，又达到了你追求的“清晰、低负担”的境界。